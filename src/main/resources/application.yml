spring.application.name=Test_internship


spring:
  datasource:
     url: jdbc:postgresql://localhost:5432/reservations
     username: resauser
     password: resapass
  jpa:
     hibernate:
         ddl-auto: validate
     show-sql: false
  flyway:
     enabled: true

server:
  port: 8080